# Deixe vazio ou comente a linha para usar SQLite local em data/resultado_operacional.db
# Em produção (Streamlit Community Cloud), configure o mesmo valor em Secrets.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require